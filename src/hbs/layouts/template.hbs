<!DOCTYPE html>
<html>
	<head>
		<title>{{title}}</title>
		<link rel="stylesheet" href="/css/template.css">
	</head>
	<body>
		{{>header}}
		<div class="container">
			<div class="row">
				<div id="content_area">
					{{>body}}
				</div>
			</div>
		</div>
		{{>footer}}
	</body>
</html>