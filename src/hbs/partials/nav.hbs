<div class="main-menu">
	<div class="container">
		<div class="row">
			<nav class="nav col-xs-12">
				<ul class="nav list-unstyled nav--top nav--level1 flex flex-justify-content-center flex-align-items-center">
			    	{{#each pages}}
					<li class="nav__item {{#if this.isCurrentPage}} nav__item--active{{/if}}">
			        	<a href="{{relative dest this.dest}}" class="nav__link">{{ data.title }}</a>
					</li>
			     	{{/each}}
				</ul>
			</nav>
		</div>
	</div>
</div>