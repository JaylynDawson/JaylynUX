<nav class="nav nav--top">
	<ul class="nav nav--level1">
    	{{#each pages}}
		<li class="nav__item">
        	<a href="{{relative dest this.dest}}" class="{{#if this.isCurrentPage}} nav__link--active{{/if}} nav__link">{{ data.title }}</a>
		</li>
     	{{/each}}
	</ul>
</nav>